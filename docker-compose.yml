version: '3.8'

services:
  sutone-agent-bok-front:
    container_name: sutone-agent-bok-front
    image: sudonghao/sutone-agent-bok-front:1.0
    restart: always
    ports:
      - "3000:3000"
    environment:
      # Also set it as runtime environment variable (for server-side rendering if applicable)
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-http://192.168.1.110:8091/api/v1}
      - NODE_ENV=production
